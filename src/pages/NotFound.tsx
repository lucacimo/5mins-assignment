import { Box } from "@chakra-ui/react";

const NotFound = () => (
  <Box
    height="80vh"
    px={4}
    py={4}
    display="flex"
    alignItems="center"
    justifyContent="center"
  >
    Not found
  </Box>
);

export default NotFound;
